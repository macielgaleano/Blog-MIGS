<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <a class="navbar-brand text-white" href="#">MIGS</a>
    <button
      class="navbar-toggler text-white"
      type="button"
      data-toggle="collapse"
      data-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon text-white"></span>
    </button>

    <div
      class="collapse navbar-collapse text-white"
      id="navbarSupportedContent"
    >
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link text-white" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="/">Articulos</a>
        </li>
        <li class="nav-item text-white">
          <a class="nav-link text-white" href="/admin">Admin</a>
        </li>
      </ul>
      <div>
        <% if (user.length >= 1) { %>
        <a class="nav-link text-white" href="/logout">Logout</a>
        <p class="">Hola, <%- user[0].nombre %> <%- user[0].apellido %></p>

        <% } else { %>
        <a class="nav-link text-white" href="/admin">Login</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
